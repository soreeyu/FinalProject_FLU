<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="ScheduleMapper">
  
  	<select id="createS" statementType="CALLABLE" parameterType="java.util.Map">
		{ call add_schedule(#{projectNum,mode=IN},#{startDate,mode=IN},#{finishDate,mode=IN},#{result, mode=OUT, jdbcType=INTEGER, javaType=java.lang.Integer},#{scheduleNum, mode=OUT, jdbcType=INTEGER, javaType=java.lang.Integer}) } 
	</select>
	
	<insert id="addPart" parameterType="com.flu.schedule.client.SchedulePartDTO">
		insert into part (schedulenum, name, startdate, finishdate, partnum) values 
		(#{scheduleNum}, #{partName}, #{partStartDate}, #{partFinishDate}, #{partNum})
	</insert>
	
	<update id="updatePart" parameterType="com.flu.schedule.client.SchedulePartDTO">
		update part set name=#{partName}, startdate=#{partStartDate}, finishdate=#{partFinishDate} 
		where schedulenum=#{scheduleNum} and partnum=#{partNum}
	</update>
	
	<delete id="deletePart" parameterType="com.flu.schedule.client.SchedulePartDTO">
		delete * part where schedulenum=#{scheduleNum} and partnum=#{partNum}
	</delete>
  	

  </mapper>