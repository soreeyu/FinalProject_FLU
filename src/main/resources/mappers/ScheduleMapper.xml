<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="ScheduleMapper">
  	<select id="addS" statementType="CALLABLE" parameterType="java.util.Map">
		{ call add_schedule(#{projectNum,mode=IN},#{result, mode=OUT, jdbcType=INTEGER, javaType=java.lang.Integer}) } 
	</select>
  	
  	<select id="addcheck" parameterType="com.flu.schedule.client.ScheduleMainDTO" resultType="com.flu.project.ProjectDTO">
		select * from project where state='진행중' and projectNum=#{projectNum}
	</select>
  </mapper>